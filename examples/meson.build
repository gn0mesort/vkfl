if get_option('enable_examples')
  executable('basic-use', files('basic_use.cpp'), dependencies: [ vkfl_dep, vulkan_dep ])
  executable('global-use', files('global_use.cpp'), dependencies: [ vkfl_dep, vulkan_dep ])
endif
